#include "holberton.h"
/**
 * mul - is a program that multiplies tow integers.
 * @a: is firts digit
 * @b: is second digit
 * Return: result of multiplication
 */
int mul(int a, int b)
{
		return (a * b);
}
