#include <stdio.h>

/**
 *main - Entry point
 *
 * print out lc letter alpha
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	int lowercase = 97;
	int lastcase = 26;
		for (i = 0; i < lastcase; i++
		{
			putchar(lowercase + i);
		}
		putchar('\n');
		return (0);
}
